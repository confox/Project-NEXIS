import React from 'react';
import ReactDOM from 'react-dom';
import NexisApp from './NexisApp';

ReactDOM.render(
  <React.StrictMode>
    <NexisApp />
  </React.StrictMode>,
  document.getElementById('root')
);
